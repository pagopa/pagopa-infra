{
  "template": {
    "settings": {
      "index": {
        "lifecycle": {
          "name": "${name}"
        },
        "default_pipeline": "${name}"
      }
    }
  },
  "_meta": {
    "description": "Settings for ${name}"
  }
}