<policies>
  <inbound>
    <base/>
    <!-- the following is the default baseUrl -->
    <set-variable name="baseUrl" value="${base-url}" />
  </inbound>
  <backend>
    <base/>
  </backend>
  <outbound>
    <base/>
  </outbound>
  <on-error>
    <base/>
  </on-error>
</policies>
