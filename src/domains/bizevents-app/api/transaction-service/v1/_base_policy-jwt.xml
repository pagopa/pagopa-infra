<policies>
    <inbound>
        <base />
        <include-fragment fragment-id="jwt-chk-wallet-session" />
        <choose>
            <when condition="@((string)context.Variables.GetValueOrDefault("xUserId","") != "")">


                <!-- Post Token PDV : START Find PII -->
                <send-request ignore-error="true" timeout="10" response-variable-name="pdf-findpii" mode="new">
                    <set-url>${pdv_api_base_path}/tokens/@((string)context.Variables.GetValueOrDefault("xUserId",""))/pii</set-url>
                    <set-method>GET</set-method>
                    <set-header name="x-api-key" exists-action="override">
                        <value>{{wallet-session-personal-data-vault-api-key}}</value>
                    </set-header>
                </send-request>
                <choose>
                    <when condition="@(((IResponse)context.Variables["pdf-findpii"]).StatusCode != 200)">
                    <return-response>
                        <set-status code="502" reason="Bad Gateway" />
                    </return-response>
                    </when>
                </choose>

                <set-variable name="pdfFindPII" value="@(((IResponse)context.Variables["pdf-findpii"]).Body.As<JObject>())" />
                <set-variable name="fiscalCode" value="@((string)((JObject)context.Variables["pdfFindPII"])["pii"])" />
                <choose>
                    <when condition="@(String.IsNullOrEmpty((string)context.Variables["fiscalCode"]))">
                        <return-response>
                            <set-status code="502" />
                            <set-header name="Content-Type" exists-action="override">
                                <value>application/json</value>
                            </set-header>
                            <set-body>@{
                        return new JObject(
                        new JProperty("title", "Bad gateway - Invalid PDV response"),
                        new JProperty("status", 502),
                        new JProperty("detail", "Cannot tokenize fiscal code")
                        ).ToString();
                    }</set-body>
                        </return-response>
                    </when>
                </choose>
                <!-- Post Token PDV : END Find PII -->


                <set-header name="Ocp-Apim-Subscription-Keykey" exists-action="override">
                    <value>{{list-trx-for-io-api-key}}</value>
                </set-header>
                <set-header name="x-fiscal-code" exists-action="override">
                    <value>@((string)context.Variables.GetValueOrDefault("fiscalCode",""))</value>
                </set-header>
            </when>
        </choose>
        <set-backend-service base-url="https://${hostname}/pagopa-biz-events-service" />
    </inbound>
    <outbound>
        <base />
    </outbound>
    <backend>
        <base />
    </backend>
    <on-error>
        <base />
    </on-error>
</policies>
