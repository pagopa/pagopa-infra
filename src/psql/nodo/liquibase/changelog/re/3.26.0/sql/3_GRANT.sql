-------------------------------------------------------------------
GRANT ALL ON TABLE ${schema}.re TO ${schema};
-------------------------------------------------------------------
ALTER TABLE IF EXISTS ${schema}.re OWNER to ${schema};
-------------------------------------------------------------------
