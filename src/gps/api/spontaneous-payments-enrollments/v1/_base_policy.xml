<policies>
    <inbound>
        <set-variable name="sub" value="@(context.Subscription.Name)" />
        <set-variable name="subid" value="@(context.Subscription.Id)" />
        <set-variable name="subkey" value="@(context.Subscription.Key)" />
        <set-variable name="organizationFiscalCode" value="@{
            return context.Variables.ContainsKey("User.Id") ? context.User.Id : "Unknown" ;
        }" />
        <base />
        <!-- /organizations/{organizationFiscalCode} -->
        <rewrite-uri template="@("/organizations/"+(string)context.Variables["organizationFiscalCode"])" copy-unmatched-params="true" />
        <set-backend-service base-url="https://weudev.gps.internal.dev.platform.pagopa.it/pagopa-spontaneous-payments-service" />
    </inbound>
    <outbound>
        <base />
    </outbound>
    <backend>
        <base />
    </backend>
    <on-error>
        <base />
    </on-error>
</policies>