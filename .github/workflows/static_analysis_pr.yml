name: Static Analysis PR

on:
  push:
    branches-ignore:
      - main

jobs:
  static_analysis:
    runs-on: ubuntu-latest

    steps:
      - run: |
          ls -la

      - name: Checkout Modified Paths
        uses: ./.github/actions/checkout-modified-paths/action.yml
